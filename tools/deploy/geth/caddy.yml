version: '3'

services:
  caddy:
    image: abiosoft/caddy
    container_name: caddy
    restart: always
    ports:
      - 8546:8546
    volumes:
      - /root/Caddyfile:/etc/Caddyfile
      - /root/.local/caddy:/root/.caddy
    environment:
      ACME_AGREE: 'true'
