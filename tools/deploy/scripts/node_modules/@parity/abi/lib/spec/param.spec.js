"use strict";
// Copyright 2015-2019 Parity Technologies (UK) Ltd.
// This file is part of Parity.
//
// SPDX-License-Identifier: MIT
Object.defineProperty(exports, "__esModule", { value: true });
var param_1 = require("./param");
describe('spec/Param', function () {
    describe('constructor', function () {
        var param = new param_1.default('foo', 'uint');
        it('sets the properties', function () {
            expect(param.name).toEqual('foo');
            expect(param.kind.type).toEqual('uint');
        });
    });
    describe('toParams', function () {
        it('maps an array of params', function () {
            var params = param_1.default.toParams([{ name: 'foo', type: 'uint' }]);
            expect(params.length).toEqual(1);
            expect(params[0].name).toEqual('foo');
            expect(params[0].kind.type).toEqual('uint');
        });
        it('converts only if needed', function () {
            var _params = param_1.default.toParams([{ name: 'foo', type: 'uint' }]);
            var params = param_1.default.toParams(_params);
            expect(params.length).toEqual(1);
            expect(params[0].name).toEqual('foo');
            expect(params[0].kind.type).toEqual('uint');
        });
    });
});
