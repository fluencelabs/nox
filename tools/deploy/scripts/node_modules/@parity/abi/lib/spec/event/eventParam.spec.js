"use strict";
// Copyright 2015-2019 Parity Technologies (UK) Ltd.
// This file is part of Parity.
//
// SPDX-License-Identifier: MIT
Object.defineProperty(exports, "__esModule", { value: true });
var eventParam_1 = require("./eventParam");
describe('spec/event/EventParam', function () {
    describe('constructor', function () {
        it('sets the properties', function () {
            var param = new eventParam_1.default('foo', 'uint', true);
            expect(param.name).toEqual('foo');
            expect(param.kind.type).toEqual('uint');
            expect(param.indexed).toBe(true);
        });
        it('uses defaults for indexed', function () {
            expect(new eventParam_1.default('foo', 'uint').indexed).toBe(false);
        });
    });
    describe('toEventParams', function () {
        it('maps an array of params', function () {
            var params = eventParam_1.default.toEventParams([{ name: 'foo', type: 'uint' }]);
            expect(params.length).toEqual(1);
            expect(params[0].indexed).toBe(false);
            expect(params[0].name).toEqual('foo');
            expect(params[0].kind.type).toEqual('uint');
        });
    });
});
