version: '3.4'
services:
  ipfs:
    container_name: ipfs
    image: ipfs/go-ipfs:release
    restart: always
    volumes:
      - /root/.ipfs/data:/data/ipfs
    ports:
      - 4001:4001 # IPFS swarm
      - 8080:8080 # HTTP endpoint
      - 5001:5001 # IPFS API
