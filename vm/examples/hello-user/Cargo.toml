[package]
name = "hello_user"
version = "0.1.1"
authors = ["Fluence Labs"]
publish = false
description = "Just a demo application for Fluence"
edition = "2018"

[lib]
name = "hello_user"
path = "src/lib.rs"
crate-type = ["cdylib"]

[dependencies]
fluence = { version = "0.0.10", features = ["export_allocator"] }
