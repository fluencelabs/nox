[package]
name = "hello_world_without_sdk"
version = "0.1.0"
authors = ["Fluence Labs"]
publish = false
description = "Just a demo application for Fluence without sdk"
edition = "2018"

[lib]
name = "hello_world_without_sdk"
path = "src/lib.rs"
crate-type = ["cdylib"]
