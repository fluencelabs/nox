[package]

name = "counter"
version = "0.1.0"
authors = ["Fluence Labs"]
publish = false

[profile.release]

opt-level = 3
lto = true

[lib]

name = "counter"
path = "src/lib.rs"
crate-type = ["cdylib"]

[dependencies]

fluence_sdk = "0.0.4"
