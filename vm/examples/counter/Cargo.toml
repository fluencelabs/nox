[package]
name = "counter"
version = "0.1.0"
authors = ["C.Solovev <constantine@fluence.one>"]

[lib]
name = "counter"
path = "src/main/rust/lib.rs"
target = "wasm32-unknown-unknown"
crate-type = ["cdylib"]

[dependencies]
