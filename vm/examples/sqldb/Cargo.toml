[package]
name = "sqldb"
version = "0.1.0"
authors = ["C.Solovev <constantine@fluence.one>"]

[lib]
name = "sqldb"
path = "src/main/rust/lib.rs"
crate-type = ["cdylib"]

[dependencies]

lazy_static = "1.1.0"
