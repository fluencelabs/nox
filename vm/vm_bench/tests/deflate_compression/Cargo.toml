[package]
name = "snappy_compression"
version = "0.1.0"
authors = ["M. Voronov <michail@fluence.one>"]

[lib]
crate-type = ["cdylib"]

[dependencies]
deflate = "0.7.19"
rand = "0.6.1"
rand_isaac = "0.1.0"
