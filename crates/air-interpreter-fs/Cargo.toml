[package]
name = "air-interpreter-fs"
version = "0.1.0"
authors = ["Fluence DAO", "Cloudless Labs"]
edition = "2021"

[dependencies]
air-interpreter-wasm = { workspace = true }
eyre = { workspace = true }
