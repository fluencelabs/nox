[package]
name = "local-vm"
version = "0.1.0"
authors = ["Fluence Labs"]
edition = "2018"

[dependencies]
ivalue-utils = { path = "../ivalue-utils" }
services-utils = { path = "../services-utils" }
host-closure = { path = "../host-closure" }
particle-protocol = { path = "../../particle-protocol" }
now-millis = { path = "../now-millis"}
uuid = { path = "../uuid" }
fs-utils = { path = "../fs-utils"}

air-interpreter-wasm = "0.9.14"
avm-server = "0.7.0"

libp2p = { version = "0.36.1", package = "fluence-fork-libp2p", features = ["tcp-tokio"] }

fstrings = "0.2.3"
serde_json = "1.0.64"
parking_lot = "0.11.1"
log = "0.4.14"
