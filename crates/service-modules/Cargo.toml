[package]
name = "service-modules"
version = "0.1.0"
authors = ["Fluence Labs"]
edition = "2018"

[dependencies]
fs-utils = { path = "../fs-utils" }

eyre = { workspace = true }
serde = { version = "1.0.118", features = ["derive"] }
blake3 = "1.3.1"
faster-hex = "0.6.0"
maplit = "1.0.2"
serde_json = "1.0.64"
