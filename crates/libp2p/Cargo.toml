[package]
name = "fluence-libp2p"
version = "0.0.1"
authors = ["Fluence Labs"]
edition = "2018"

[dependencies]
libp2p = "0.19.0"
parity-multiaddr = "0.9.0"
multihash ="0.11.0"
futures = "0.3.4"
futures-util = "0.3.4"
async-std = "1.5.0"
itertools = "0.9.0"

serde = { version = "1.0.110", features = ["derive"] }
serde_json = "1.0.53"
bs58 = "0.3.0"

failure = "0.1.6"
base64 = "0.11.0"
log = "0.4.8"

[dev-dependencies]
rand = "0.7.3"
