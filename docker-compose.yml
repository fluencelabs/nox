services:
  nox:
    build: .

  aurora:
    image: fluencelabs/chain-rpc:latest
    ports:
      - 8545:8545

  ipfs:
    image: ipfs/go-ipfs
    ports:
      - 5001:5001
      - 4001:4001
    environment:
      IPFS_PROFILE: server
