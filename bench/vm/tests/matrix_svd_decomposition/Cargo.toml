[package]
name = "matrix_svd_decomposition"
version = "0.1.0"
authors = ["Fluence Labs"]

[lib]
crate-type = ["cdylib"]

[dependencies]
nalgebra = { version = "0.16", features = [ "alloc" ] }
rand = "0.6.1"
rand_isaac = "0.1.0"
