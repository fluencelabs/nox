[package]
name = "matrix_qr_decomposition"
version = "0.1.0"
authors = ["M. Voronov <mikhail@fluence.one>"]

[lib]
crate-type = ["cdylib"]

[dependencies]
nalgebra = { version = "0.16", features = [ "alloc" ] }
rand = "0.6.1"
rand_isaac = "0.1.0"
