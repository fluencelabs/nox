fluence {

  directory = ${user.home}/fluence
  keyPath = ${fluence.directory}/key.pk8

  network {
    kademlia {
      maxBucketSize: 16
      maxSiblingsSize: 32
      parallelism: 3
      pingExpiresIn: 1 second
    }
  }

  gitHash = "-"
  gitHash = ${?FLUENCE_GIT_HASH}
}
